# 実際の値は .env に置く（.env はGit管理外）。ここにはプレースホルダーのみを書く。
# EXAMPLE_API_KEY=your-api-key-here

# --- GitHub認証のWorkspace限定override（正本: tools/REFERENCE.md#GitHub認証Tool） ---
# 標準は1台につき一度のtools/setup-github-auth.shで作るマシン共通credential。
# この値は特定Workspaceだけを一時的に上書きするときに限り使い、通常は空のままにする。
GH_TOKEN=
