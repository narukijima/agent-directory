# 実際の値は .env に置く（.env はGit管理外）。ここにはプレースホルダーのみを書く。
# EXAMPLE_API_KEY=your-api-key-here

# --- Agent固有GitHub認証（正本: tools/REFERENCE.md#GitHub認証Tool） ---
# Agentごとに別のfine-grained PATを、そのAgent rootの.envだけへ設定する。
# OS home、machine共通store、別Agent、global environmentへ置かない。
GH_TOKEN=
