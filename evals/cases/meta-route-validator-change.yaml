name: meta-route-validator-change

request: |
  validatorへKnowledgeの状態検査を追加して。

expect:
  route: meta
  must_read:
    - AGENTS.md
    - tools/TOOLS.md
    - tools/validate-agent-directory.sh
    - evals/EVALS.md
  may_write:
    - tools/**
    - evals/**
  must_not_write:
    - knowledge/raw/internal/**
    - knowledge/raw/external/**
