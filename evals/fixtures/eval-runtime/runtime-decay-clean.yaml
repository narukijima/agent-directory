name: runtime-decay-clean
fixture: eval-runtime/workspace
decay_pair: runtime
decay_variant: clean
decay_stale_check: true

request: |
  同じ現在情報を確認して。

expect:
  route: meta
  max_read_files: 12
  max_context_bytes: 32768
  max_escalations: 0
  must_read:
    - AGENTS.md
  must_not_reference:
    - knowledge/wiki/topics/stale.md
